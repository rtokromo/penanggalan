import { 
    REFERENCE_DATES,
    JAVANESE_YEAR_LENGTH 
} from '../config/constants';

export function convertToJavaneseDate(gregorianDate) {
    // Implementatie conversie logica
}

export function convertToGregorianDate(javaneseYear, javaneseMonth, javaneseDay) {
    // Implementatie conversie logica
}
